{% extends 'base.html.twig' %}

{% block title %}Mcommande{% endblock %}

{% block body %}
    <h1>Mcommande</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ mcommande.id }}</td>
            </tr>
            <tr>
                <th>Info</th>
                <td>{{ mcommande.info }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_mcommande_index') }}">back to list</a>

    <a href="{{ path('app_mcommande_edit', {'id': mcommande.id}) }}">edit</a>

    {{ include('mcommande/_delete_form.html.twig') }}
{% endblock %}
