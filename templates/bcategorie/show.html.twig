{% extends 'base.html.twig' %}

{% block title %}Bcategorie{% endblock %}

{% block body %}
    <h1>Bcategorie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bcategorie.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ bcategorie.nom }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_bcategorie_index') }}">back to list</a>

    <a href="{{ path('app_bcategorie_edit', {'id': bcategorie.id}) }}">edit</a>

    {{ include('bcategorie/_delete_form.html.twig') }}
{% endblock %}
